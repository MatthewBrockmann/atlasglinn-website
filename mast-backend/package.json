{
  "name": "mast-booking-backend",
  "version": "1.1.0",
  "private": true,
  "description": "Cloudflare Worker for MAST Solutions registration: eligibility screening, the Class Participation Agreement, refund-policy consent, Stripe Checkout, D1 persistence and Resend email.",
  "type": "module",
  "scripts": {
    "test": "node test-worker.mjs",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute mast_bookings --remote --file=schema.sql"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
