# Assets

- `class-participation-agreement.pdf` — the fillable Class Participation and Use of
  Property Agreement (sha256 prefix `81961f2a07675eff`, the `agreement_version`).

The **range directions** are deliberately NOT in this repository: the repo is
public and the range address is private. They live in the Worker secret
`RANGE_DIRECTIONS` (Markdown, ~2 KB) alongside `RANGE_ADDRESS` and
`RANGE_COORDS`, and are emailed only after a confirmed booking.
